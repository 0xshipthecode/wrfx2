(ns raws-src-def
  (use joxa-records))

;; name - an identifier for the raws source definition
;; var-list - list of variables provided by the raws source
;; max-dl - maximum concurrent downloads allowed by the system

(defrecord+ name var-list max-dl)
