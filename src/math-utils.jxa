(ns math-utils
    (require (erlang :as erl)
	     (joxa-core :as jxc)))

(defn+ sign (val)
  "Return sign of number, 0 -> 0, x < 0 -> -1, x > 0 -> 1."
  (case val
	(0 0)
	(v (when (erl/< v 0)) -1)
	(v 1)))


(defn+ zero? (val)
  "Check if a value is zero."
  (case val
	(0 :true)
	(_ :false)))
