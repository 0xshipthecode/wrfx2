(ns grib-app
    (require grib-man
	     msglog))



(defn+ start (stor-dir)
  (let* (log-pid (msglog/start-log "grib-system" [(msglog/make-target :device :info "log/grib-system.log")])
	 log-f   (msglog/make-log-func log-pid))
    (grib-man/start-link stor-dir log-f)))

		 