(ns grib-app
    (require grib-man
       log-srv
       (joxa-core :as jxc)))


(defn+ start (stor-dir)
  (log-srv/start-link "log")
  (log-srv/create-log "grib-system" [])
  (grib-man/start-link stor-dir (log-srv/get-log-f "grib-system")))

		 
